# Follow the official LongMemEval retrieval protocol

The LongMemEval Retrieval Baseline runs this project's E5, BM25, reciprocal-rank-fusion, and BGE retrieval variants over the pinned official cleaned LongMemEval-S data while following the official retrieval implementation's session indexing, user-only text, case eligibility, and `@5`/`@10` scoring rules. Each haystack array position remains a distinct Haystack Session Occurrence during retrieval, fusion, and reranking even when multiple positions share one Source Session ID; only the metric boundary projects occurrences back to those possibly repeated labels, matching the official implementation's position-based rankings instead of deduplicating its corpus. Abstention and cases without user-side target evidence remain in raw artifacts with exclusion reasons but do not contribute to aggregate retrieval metrics. Reader-LLM generation, answer judging, assistant-side retrieval coverage, the official repository's retriever models, and claims of an official leaderboard score are outside this baseline; ADR-0011's role-complete variant is superseded so the primary baseline stays comparable to the official retrieval experiment.
